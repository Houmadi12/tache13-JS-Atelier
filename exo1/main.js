// Comment déclarer un tableau en JS

let tableau = ["String",12];
 
// Ajouter une valeur à la fin du tableau
tableau.push("New value")

// Afficher le tableau dans le console
console.log(tableau);

// Suprimer la dernier valeur du tableau
tableau.pop();

// Afficher les tableau dans le console
console.log(tableau);